# Local dev requires no env values.
# These defaults are used automatically:
DB_CLIENT=sqlite
SQLITE_PATH=./.data/moviescore.db

# For production or local postgres testing:
# DB_CLIENT=postgres
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/moviescore
